<p style="color: green"><%= notice %></p>

<%= render @pst %>

<div>
  
  <% if policy(@pst).update? %>
  <%= link_to "Edit this pst", edit_pst_path(@pst) %> |
  <%= button_to "Destroy this pst", @pst, method: :delete %>
    
  <% end %>
  
  <%= link_to "Back to psts", psts_path %>

</div>
